import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Removed experimental optimizePackageImports for tailwindcss as it conflicts with v4 PostCSS pipeline */
};

export default nextConfig;
